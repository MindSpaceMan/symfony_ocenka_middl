### db: orm, mysql
### linter - psalm
### tests - phpunit
### запуск:
```
composer install
установить подключение к своей версии mysql 
через смену данных в .env файле
symfony d:d:c
symfony d:m:m
symfony serve
``` 

p.s.: docker, nginx и любое дополнительное окружение не ставил,
так как этого не было явно указано в задании. 
### любую тему могу освоить, стоит только 2-3 раза
### попрактиковаться с помощью примеров в сети. 
